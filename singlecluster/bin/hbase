#!/usr/bin/env bash

# Load settings
root=$(cd "$(dirname "$0")/.." && pwd)
bin=${root}/bin

# shellcheck source=/dev/null
. "${bin}/gphd-env.sh"

pushd "${HBASE_ROOT}" &>/dev/null || exit 1
bin/hbase "$@"
popd &>/dev/null || exit 1
